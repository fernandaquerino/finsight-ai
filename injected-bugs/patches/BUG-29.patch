diff --git a/src/features/dashboard/month.ts b/src/features/dashboard/month.ts
index 49271ca..1d860f6 100644
--- a/src/features/dashboard/month.ts
+++ b/src/features/dashboard/month.ts
@@ -16,7 +16,7 @@ export function parseMonthParam(
 ): MonthParam {
   if (value && /^\d{4}-\d{2}$/.test(value)) {
     const [year, month] = value.split("-").map(Number);
-    if (year && month && month >= 1 && month <= 12) {
+    if (year && month && month >= 1 && month < 12) {
       return { year, month };
     }
   }
