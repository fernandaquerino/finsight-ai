import { AccountScreen } from "@/features/settings/components/AccountScreen";

export const metadata = {
  title: "Minha conta · FinSight AI",
  description: "Perfil, contas cadastradas e segurança.",
};

export default function AccountPage() {
  return <AccountScreen />;
}
